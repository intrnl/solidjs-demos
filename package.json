{
  "private": true,
  "name": "solidjs-demos",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "publish": "rm -r dist; pnpm run build; scripts/publish.sh"
  },
  "dependencies": {
    "perf-monitor": "^0.4.1",
    "solid-js": "^1.6.1"
  },
  "devDependencies": {
    "vite": "^3.2.3",
    "vite-plugin-solid": "^2.4.0"
  }
}